warning('off','all');

tic;
image = double(imread('4.jpg'))/255;

image = imresize(image, 0.4);

result = dehaze_fast(image, 0.95, 5);
toc;


figure, imshow(result);title('Dehazed ')

warning('on','all');